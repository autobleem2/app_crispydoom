diff --git a/CMakeLists.txt b/CMakeLists.txt
index f2f41e0e..ea585527 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -37,7 +37,8 @@ option(ENABLE_SDL2_NET "Enable SDL2_net" On)
 option(ENABLE_SDL2_MIXER "Enable SDL2_mixer" On)
 
 if (NOT DEFINED EMSCRIPTEN)
-    find_package(SDL2 2.0.14)
+    # AutoBleem: 2.0.12 - the console's SDL until 2026-09-23 (still the release image's); nothing newer is called
+    find_package(SDL2 2.0.12)
 endif()
 if(ENABLE_SDL2_MIXER)
     if (NOT DEFINED EMSCRIPTEN)
